type: object
required:
  - total
  - limit
  - offset
properties:
  total:
    type: integer
    description: Total number of elements matching the filter criteria
  limit:
    type: integer
    description: Limit used for the query
  offset:
    type: integer
    description: Offset used for the query