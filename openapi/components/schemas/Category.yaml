allOf:
  - $ref: ./CategoryRequest.yaml
  - type: object
    properties:
      id:
        type: string
        description: Unique identifier for the category
        example: cat123
      active:
        type: boolean
        description: Whether the category is active
        example: true
    required:
      - id
